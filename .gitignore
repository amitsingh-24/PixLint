# Virtual environments
.venv/
venv/
env/

# Python bytecode / caches
__pycache__/
*.pyc
*.pyo
*.pyd

# Build / packaging artifacts
build/
dist/
*.egg-info/
*.egg

# Test / lint / type caches & reports
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
bandit*.json

# Runtime / security logs
*.log
pixlint_security.log

# Secrets & local env (NEVER commit these)
.env
.env.*
*.pem
*.key
credentials.json

# Maintainer-only docs — kept local, excluded from GitHub/PyPI/all releases
docs/PUBLISHING.md
docs/DEPLOYMENT.md

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# uv lockfile (regenerable; excluded to avoid shipping a stale one)
uv.lock

# Local-only dev files (not for the public repo)
plan.md
test_client.py
test_runner/
